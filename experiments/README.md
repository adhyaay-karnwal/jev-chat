# Live Jev ablations

```bash
uv run --with matplotlib python experiments/bench.py
```

Writes `results/runs.json`, `results/summary.json`, and `paper/figures/*`.
Hits `jev-latest`. Eight prompts, four strategies. Not a leaderboard.
